package Packages_example.copy;

public class cal {

	public int add(int n1,int n2) {
		return n1+n2;
	}
		/*
		 * packages are basically used to organize the code 
		 * using packages all java files can be managed in one folder
		 * folder inside another folder is called as sub folder.
		 */
	
	/*
	 * we generally make use system.out.println() statement in order to print something
	 * but system is also a predefined then why we don't import any package
	 * well in this case system class belong to java.lang package
	 * so this java.lang package is implicitly mentioned in every java file
	 * if we don't mention then also it is present.
	 */
		
	
}
