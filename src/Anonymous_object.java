
public class Anonymous_object {

	public static void main(String[] args) {
		/*
		 * object which is referenced by certain variable then it is called as referenced object
		 * when a object is not referenced by any variable then it is called as anonymous object
		 * we cannot make use of anonymous object again and again this is only drawback
		 * even if we try to make use anonymous object then also new object is been created
		 */
		System.out.println(new Anonymous_object());
		/*
		 * In java we usually follow camel casing naming conventions
		 * why do even we follow naming convention
		 * in order to make code more readable.
		 * what is syntax it is basically a some rules and protocols which every programming language follows
		 * Evey classes interfaces are represented using captial letters
		 * Every variables and methods are represented using lower case in java
		 * 
		 */
	}
}
